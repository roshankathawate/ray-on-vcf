---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: vmraynodeconfigs.vmray.broadcom.com
spec:
  group: vmray.broadcom.com
  names:
    kind: VMRayNodeConfig
    listKind: VMRayNodeConfigList
    plural: vmraynodeconfigs
    singular: vmraynodeconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VMRayNodeConfig is the Schema for the vmraynodeconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: The configuration of the VMRayVirtualMachine
            properties:
              cloud_init_config:
                description: Config map name that stores the cloud init config
                type: string
              content_library:
                description: Name of the Content Library where the OVF resides.
                type: string
              network_policy:
                description: Network policy
                type: string
              nfs:
                description: The NFS location that should be mounted to the Ray nodes.
                type: string
              ovf:
                description: The OVF file from which the Ray cluster's nodes will
                  be created.
                type: string
              storage_policy:
                description: Storage policy
                type: string
              vm_class:
                description: The VM class for Ray nodes
                type: string
            required:
            - content_library
            - ovf
            - storage_policy
            - vm_class
            type: object
          status:
            description: The current status of the VMRayVirtualMachine
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
